<ui:FluentWindow 
    x:Class="DocMind.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:DocMind"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:DocMind"
        mc:Ignorable="d"
        Title="DocInsight" Height="1080" Width="1920"
        WindowStartupLocation="CenterScreen"
    Background="#E1E8F0"
        WindowStyle="None"
        AllowsTransparency="False"
    ResizeMode="CanResize"
        >

    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            ResizeBorderThickness="6"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:StringIsNotNullOrEmptyConverter x:Key="StringIsNotNullOrEmptyConverter"/>
        <local:BooleanToHorizontalAlignmentConverter x:Key="BooleanToHorizontalAlignmentConverter"/>
        <local:BooleanToBrushConverter x:Key="BooleanToBrushConverter"/>
        <local:EqualsConverter x:Key="EqualsConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:FilePreviewTemplateSelector x:Key="FilePreviewTemplateSelector">
            <local:FilePreviewTemplateSelector.TxtTemplate>
                <DataTemplate>
                    <RichTextBox x:Name="OriginalTextBox"
                                 Padding="32,25"
             VerticalScrollBarVisibility="Auto"
         IsReadOnly="True" 
         BorderThickness="0"                            
         Background="Transparent"
         FontSize="16"
         >
                        <i:Interaction.Behaviors>
                            <behaviors:FlowDocumentBehavior BoundDocument="{Binding Display}"/>
                        </i:Interaction.Behaviors>

                    </RichTextBox>
                </DataTemplate>
            </local:FilePreviewTemplateSelector.TxtTemplate>

            <local:FilePreviewTemplateSelector.WordTemplate>
                <DataTemplate>
                    <DocumentViewer Document="{Binding Display}" Margin="10"/>
                </DataTemplate>
            </local:FilePreviewTemplateSelector.WordTemplate>

            <local:FilePreviewTemplateSelector.ExcelTemplate>
                <DataTemplate>
                    <DataGrid 
                        AutoGenerateColumns="True" 
                        Margin="10"
                        ItemsSource="{Binding Display.DefaultView}"
                        CanUserAddRows="False"
                        HorizontalAlignment="Stretch"
                        ColumnWidth="*"
                        Width="auto"/>

                </DataTemplate>
            </local:FilePreviewTemplateSelector.ExcelTemplate>

            <local:FilePreviewTemplateSelector.DefaultTemplate>
                <DataTemplate>
                    <StackPanel HorizontalAlignment="Center" 
                VerticalAlignment="Center">
                        <ui:SymbolIcon Symbol="DocumentSearch24" 
                       FontSize="48" 
                       Margin="0,0,0,12"
                       Foreground="#CCCCCC"/>

                        <TextBlock Text="请先选择要预览的文档" 
                   FontSize="14"
                   HorizontalAlignment="Center"
                   Foreground="#999999"/>

                        <TextBlock Text="点击左侧按钮进行上传" 
                   FontSize="12"
                   Margin="0,8,0,0"
                   HorizontalAlignment="Center"
                   Foreground="#BBBBBB"/>
                    </StackPanel>
                </DataTemplate>
            </local:FilePreviewTemplateSelector.DefaultTemplate>
        </local:FilePreviewTemplateSelector>

        <Storyboard x:Key="BaseAnimation" RepeatBehavior="Forever">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
                <EasingDoubleKeyFrame KeyTime="0" Value="0.2"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.4" Value="1.0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.8" Value="0.2"/>
                <EasingDoubleKeyFrame KeyTime="0:0:1.0" Value="0.2"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <Storyboard x:Key="AnimateDot1" RepeatBehavior="Forever">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
                <EasingDoubleKeyFrame KeyTime="0" Value="0.2"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.4" Value="1.0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.8" Value="0.2"/>
                <EasingDoubleKeyFrame KeyTime="0:0:1.0" Value="0.2"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <Storyboard x:Key="AnimateDot2" RepeatBehavior="Forever" BeginTime="0:0:0.2">
        </Storyboard>

        <Storyboard x:Key="AnimateDot3" RepeatBehavior="Forever" BeginTime="0:0:0.4">
        </Storyboard>

    </ui:FluentWindow.Resources>

    <Grid  VerticalAlignment="Stretch">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
        Grid.Column="0" 
        Grid.ColumnSpan="2"
        Background="Transparent"
        MouseLeftButtonDown="OnDragWindow">

            <Grid>
                <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center">

                    <ui:Button 
                        Margin="0 5 10 0"
                        Height="30"
                        Width="30"
                        Icon="{ui:SymbolIcon Symbol=Dismiss24}"
                               Background="#2B2B2B"
                        Foreground="#E6E6E6"
                        
                       Click="OnCloseApp"
                       ToolTip="关闭" >

                    </ui:Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid  Grid.Row="1" Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <ui:Card Grid.Row="0" 
                     BorderBrush="#E0E0E0" BorderThickness="1"
         Margin="0,0,0,10"
         Padding="0"
         Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}">

                <!-- 给父Grid添加行定义，明确区分按钮区和文件列表区 -->
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <!-- 上传按钮行（固定高度） -->
                        <RowDefinition Height="auto"/>
                        <!-- 文件列表行（剩余空间） -->
                    </Grid.RowDefinitions>

                    <!-- 标题栏（上传按钮）放在第0行 -->
                    <Grid Grid.Row="0" Background="#F5F5F5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ui:Button 
Grid.Column="0"
Content="上传文档" 
Command="{Binding UploadFileCommand}"
                    Foreground="White"
                            Height="30"
                            Background="#6FCF97"

Icon="{ui:SymbolIcon Symbol =ArrowUpload24}"
Margin="10,0"
Padding="8,0"
                    Appearance="Dark"
FontSize="14"
FontWeight="Normal"
                    
Cursor="Hand"/>

                        <!-- 已上传数量提示 -->
                        <TextBlock 
        Grid.Column="1"
        Text="{Binding UploadedFiles.Count, StringFormat='已上传 ({0})'}"  
        VerticalAlignment="Center"
        HorizontalAlignment="Right"
        Margin="0,0,10,0"
        Foreground="#666666"/>
                    </Grid>

                    <Border Height="1" 
Background="{DynamicResource ControlStrokeColorDefaultBrush}" 
                            VerticalAlignment="Bottom"
/>

                    <ScrollViewer 
    Grid.Row="1"  
            Height="Auto" 
    MaxHeight="120" 
    VerticalScrollBarVisibility="Auto"
    HorizontalScrollBarVisibility="Disabled">

                        <ItemsControl ItemsSource="{Binding UploadedFiles}">
                            <!-- ItemTemplate保持不变 -->

                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type local:DocumentFile}">
                                    <Grid Height="36" Margin="0,2">

                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <!-- 用DataTrigger + 多值绑定 + 转换器，判断当前项是否等于SelectedFile -->
                                                    <DataTrigger Value="True">
                                                        <DataTrigger.Binding>
                                                            <MultiBinding Converter="{StaticResource EqualsConverter}">
                                                                <!-- 绑定1：当前DocumentFile项 -->
                                                                <Binding/>
                                                                <!-- 绑定2：ViewModel中的SelectedFile -->
                                                                <Binding 
                            RelativeSource="{RelativeSource AncestorType={x:Type ItemsControl}}" 
                            Path="DataContext.SelectedFile"/>
                                                            </MultiBinding>
                                                        </DataTrigger.Binding>
                                                        <!-- 选中时的样式：淡蓝色背景 -->

                                                        <Setter Property="Background" Value="#E8F1FC"/>
                                                        <!-- 文件名选中样式 -->

                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>

                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Cursor="Hand">
                                            <!-- 通过 InputBinding 绑定鼠标事件与命令 -->
                                            <StackPanel.InputBindings>
                                                <!-- 鼠标按下事件绑定 -->
                                                <MouseBinding 
        MouseAction="LeftClick"  
                                            Command="{Binding Path=DataContext.SelectFileCommand, 
                          RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
        CommandParameter="{Binding}"/>

                                            </StackPanel.InputBindings>

                                            <ui:SymbolIcon 
               Margin="12,0,8,0" 
               FontSize="16"
               VerticalAlignment="Center"
               Symbol="Document20"/>
                                            
                                          
                                            <TextBlock 
    Text="{Binding FileName}"  
    Margin="2,0,5,0"
    ToolTip="{Binding FilePath}"  
                                                Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                                                
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <!-- 默认文字样式 -->
                                                        <Setter Property="Foreground" Value="#333333"/>
                                                        <Setter Property="FontWeight" Value="Normal"/>

                                                        <!-- 选中时的文字样式 -->
                                                        <Style.Triggers>
                                                            <DataTrigger Value="True">
                                                                <DataTrigger.Binding>
                                                                    <MultiBinding Converter="{StaticResource EqualsConverter}">
                                                                        <!-- 绑定1：当前DocumentFile项 -->
                                                                        <Binding/>
                                                                        <!-- 绑定2：ViewModel中的SelectedFile -->
                                                                        <Binding 
                            RelativeSource="{RelativeSource AncestorType={x:Type ItemsControl}}" 
                            Path="DataContext.SelectedFile"/>
                                                                    </MultiBinding>
                                                                </DataTrigger.Binding>
                                                                <!-- 选中时的文字颜色+加粗 -->
                                                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>

                                                                <Setter Property="Background" Value="{DynamicResource AccentColorSecondaryBrush}"/>


                                                                <!--<Setter Property="FontWeight" Value="Medium"/>-->
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>

                                            </TextBlock>

                                            <TextBlock 
    Text="{Binding FileSizeKB, StringFormat='({0}KB)'}"  
    FontSize="12"
    Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        </StackPanel>

                                        <ui:Button 
                                            Margin="0 0 6 0"
                    Grid.Column="1"
                    Width="36" 
                    Height="32"
                    BorderThickness="1"
                                            Appearance="Transparent"
                                            Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                            Icon="{ui:SymbolIcon Symbol =Delete12}" 
                    Command="{Binding DataContext.RemoveFileCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                    CommandParameter="{Binding}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                        </ItemsControl>
                    </ScrollViewer>
                </Grid>

            </ui:Card>


            <ui:Card Grid.Row="1" VerticalContentAlignment="Stretch" VerticalAlignment="Stretch" BorderBrush="#E0E0E0" BorderThickness="1">


                <ScrollViewer  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" VerticalContentAlignment="Stretch"
          x:Name="ChatScrollViewer">
                    <ItemsControl ItemsSource="{Binding Messages}">


                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type local:Message}">
                                <!-- 单条消息容器 -->
                                <Grid Margin="5,3">
                                    <!-- 对齐方式：用户消息靠右，回复消息靠左 -->
                                    <Grid HorizontalAlignment="{Binding IsSentByUser, Converter={StaticResource BooleanToHorizontalAlignmentConverter}}">

                                        <!-- 消息气泡：用Style+DataTrigger控制样式 -->
                                        <Border 
        x:Name="MessageBorder"
        CornerRadius="8" 
        Padding="12,8" 
        BorderThickness="1">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <!-- 默认样式（回复消息） -->
                                                    <Setter Property="Background" Value="#F0F0F0"/>
                                                    <Setter Property="BorderBrush" Value="#DDDDDD"/>
                                                    <!-- 用户消息样式（DataTrigger触发） -->
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSentByUser}" Value="True">
                                                            <Setter Property="Background" Value="#5dade2"/>
                                                            <Setter Property="BorderBrush" Value="#5dade2"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <StackPanel>
                                                <TextBlock 
                Margin="4"
                FontSize="16"
                Text="{Binding Text}"  
                MinHeight="20"
                TextWrapping="Wrap">
                                    
 <b:Interaction.Behaviors>
    <local:RichTextHyperlinkBehavior JumpToPageCommand="{Binding DataContext.JumpToPageCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" />
    <local:RichTextHyperlinkBehavior JumpToParagraphCommand="{Binding DataContext.JumpToParagraphCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" />
</b:Interaction.Behaviors>

                                                </TextBlock>

                                                <!-- 时间戳：样式保留不变 -->
                                                <TextBlock 
                Text="{Binding Timestamp, StringFormat='HH:mm'}" 
                FontSize="10" 
                Margin="0,4,0,0"
                HorizontalAlignment="Right">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <!-- 默认样式（回复消息） -->
                                                            <Setter Property="Foreground" Value="#666666"/>
                                                            <!-- 用户消息时间（DataTrigger触发） -->
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSentByUser}" Value="True">
                                                                    <Setter Property="Foreground" Value="#EEEEEE"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>
                </ScrollViewer>

            </ui:Card>

            <ui:Card Grid.Row="2" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="0 10 0 0"
                     Background="#F5F5FF" BorderBrush="#D6E4FF" BorderThickness="1" Padding="12,8"
                     >

                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="5*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <ui:ProgressRing Width="14" Height="14" Margin="10,0,0,0" 
                                 IsIndeterminate="True"
                                 Visibility="{Binding IsRingVisible, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding Hint}"
                                   Margin="10 0 0 0"
                           FontSize="12" 
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" 
            Orientation="Horizontal" 
            HorizontalAlignment="Right" 
            VerticalAlignment="Center">

                        <TextBlock Text="溯源" 
               FontSize="12" 
               VerticalAlignment="Center" 
               Margin="0,0,8,0" 
               Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

                        <ui:ToggleSwitch IsChecked="{Binding IsSourcingEnabled}" 
                     ToolTip="开启后将验证答案来源，耗时稍长"
                     OnContent="" 
                     OffContent=""
                     MinWidth="0"
                     VerticalAlignment="Center"/>
                    </StackPanel>

                    <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,10,0,0"
                          
                          >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="7*" />
                            <ColumnDefinition Width="*"/>

                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="message"
                Text="{Binding UserQuestion, UpdateSourceTrigger=PropertyChanged}" 
                KeyDown="TextBox_KeyDown"
                VerticalContentAlignment="Center"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                Padding="10"/>

                        <ui:Button Grid.Column="1" 
                                HorizontalAlignment="Right"
                                Height="40"
                                Width="80"
                                   Appearance="Transparent"
                                   Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"
                                   Icon="{ui:SymbolIcon Symbol=Send24}"
Content="发送" 
Command="{Binding SendMessageCommand}"/>


                    </Grid>

                </Grid>

            </ui:Card>
            
        </Grid>

        <ui:Card
            VerticalAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            BorderBrush="#E0E0E0" BorderThickness="1"
            Margin="0 10 10 10"
    Grid.Column="1"
            Grid.Row="1">

            <Grid Background="#FAFAFA" VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 标题行 -->
                <Grid Grid.Row="0" Background="#F5F5F5">

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        
                        <ui:SymbolIcon Symbol="Document24" Margin="10,0,8,1" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"/>

                        <TextBlock
Text="{Binding SelectedFile.FileName, FallbackValue='文档预览'}"
VerticalAlignment="Center"
FontWeight="Medium"/>

                    </StackPanel>

                    
                </Grid>

                <Separator Grid.Row="1"  />
                
                <!-- 内容行 -->
                <ContentControl
                    VerticalContentAlignment="Stretch"
                    VerticalAlignment="Stretch"
            Grid.Row="2"
            x:Name="previewContentControl"
            Content="{Binding SelectedFile}"
            ContentTemplateSelector="{StaticResource FilePreviewTemplateSelector}"/>
            </Grid>

        </ui:Card>



        <Grid 
            Grid.ColumnSpan="2"
            Grid.RowSpan="2"
            x:Name="GlobalLoadingOverlay"
      Background="#40000000" 
      Panel.ZIndex="999"
      Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" 
                         Width="60" 
                         Height="60"
                         Foreground="DarkGray"/>

                <ui:TextBlock Text="{Binding LoadMessage, FallbackValue='Loading...'}"
                      Margin="0,10,0,0"
                      FontWeight="Medium"
                      Foreground="White"/>
            </StackPanel>
        </Grid>

    </Grid>


</ui:FluentWindow>
